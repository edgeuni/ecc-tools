#pragma once

#include <cstdint>

namespace idb::refactor {

// Raw entity handle shared by every object in the technology registry.
enum class TechEntity : uint32_t
{
};

}  // namespace idb::refactor
